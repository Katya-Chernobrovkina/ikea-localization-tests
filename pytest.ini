[pytest]
addopts = --alluredir=allure-results
pythonpath = .
markers =
    de: tests scoped to the German locale
    us: tests scoped to the US locale
    se: tests scoped to the Swedish locale
    localization: general localization checks
